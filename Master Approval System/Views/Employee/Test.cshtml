@model Master_Approval_System.ViewModels.TestViewModel
@{
    ViewBag.Title = "Test";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<h2>Test</h2>

@using (Html.BeginForm("test", "Employee", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <h4>Add details to add employee</h4>
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <div class="form-group">
        @Html.LabelFor(m => m.test, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.test, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.test, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.test, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.test, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.test, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Add test" class="btn btn-default" />
        </div>
    </div>
}